name: Audio
icon: file-audio
fields:
  audio:
    label: Audio
    type: files
    required: true
    multiple: false
    min: 1
    query: page.files.template('mux-audio')
    uploads:
      template: mux-audio
  caption:
    label: Caption
    type: writer
    inline: true
